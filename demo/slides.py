# The walkthrough of the Collection, second edition: what is on the shelf, how the hub and the agent work, how a
# component gets signed off, and how it all runs inside the bank. SLIDES is the authoritative script; deck.py in
# components/skills/walkthrough-video renders it. (id, kicker, title, bullets, shot, narration)
TITLE = "The Collection: walkthrough"
FOOT = "The Collection · walkthrough v2 · AI champions · internal"
SLIDES = [
    ("title", "AI champions", "The Collection, in eight minutes", ["What is on the shelf and who it is for", "How the hub, the assistant and the agent behave", "How a component earns its sign-offs", "How it runs inside the bank"], None,
     "Hello. This is a walk through the Collection as it stands now. In about eight minutes we will look at what is on the shelf and who each kind of thing is for, how the hub and the agent behave, how a component earns its two sign-offs, and how all of it runs inside the bank's own systems. Everything you will see is real: screenshots of the running hub and terminals, not mock-ups."),
    ("shape", "The shape", "Four parts, one repository", ["components: 29 building blocks in six categories", "the hub, and hub-api behind it", "an agent runtime: one agent over MCP and a run API", "the knowledge base is a separate product; we publish pages into it"], "d-architecture.png",
     "Here is the shape. On the left, people: the hub in a browser, and tools like Claude Code that speak MCP. In the middle, our two services: hub-api, which is the hub's back end, and the agent runtime, which runs one agent. On the right, the bank's systems: the identity provider, the knowledge base, the model on Bedrock, Jira and Azure DevOps, and the AWS services for keys, secrets and the audit export. Nothing on this picture reaches the public internet."),
    ("categories", "The shelf", "Six kinds of component, and who each is for", ["skill: a procedure anyone can follow; nothing to run", "tool, integration, pattern: code an engineer copies", "harness: the loop an agent runs inside", "agent: a template, its tools, and a harness"], "12-term-list.png",
     "Every component has a category, and the category tells you who it is for. Skills are written procedures, and anyone can follow one. Tools, integrations and patterns are code an engineer copies into a project. A harness is the loop an agent runs inside. And an agent is the composite: a template that says what it is and what it may call, the tools it calls, and the harness it runs in. The shelf tool prints them all with their version, stage and sign-off state."),
    ("discover", "The hub", "Discover: what you may use today", ["Assistants, agents, knowledge, tools, roads", "Access is decided per person, never typed by an owner", "Every component of the collection is listed here too"], "01-discover-agents.png",
     "The hub. Discover shows what you may use today, and lets you ask for the rest. The tabs group the catalog; here is the Agents tab. Access on each card is worked out for the signed-in person from their entitlements. The components of the collection are listed under the same tabs, so an engineer and an operator see the same shelf."),
    ("listing", "The hub", "A listing: what it does, what it may do, who signed it", ["What it can read and do, with the tier of each operation", "Its template, its tools and its harness", "The sign-off card: version, owner, AI security"], "02-listing-agent.png",
     "Open the incident first-read agent. The page says what it does, and lists what it can read and do with the tier of each operation: reading a ticket and the last deploy are reads, posting the first read is a write that waits for a person. The tiles show its three parts: the template, two tools, and the harness. On the right is the sign-off card, which we will come back to."),
    ("guide", "The hub", "The guide: a companion that knows who it is talking to", ["Asks whether you are here to build, to decide, or to use; suggests from your role", "One next step, worked out from your own briefs, sign-offs and pages", "A spotlight tour, and answers quoted from our own pages with the page named", "No tools: it points and explains, it never acts"], "16-guide-leader.png",
     "Bottom right of every page is the guide. Open it once and it asks what you are here for: to build something, to understand and decide, or to use the hub. From then on it keeps to that. It says in a line what the page is for, suggests one next step worked out from what you have actually done, walks you through the pages with the relevant part lit up, and answers questions by quoting our own documentation, naming the page each answer came from. For a leader, on an agent's page, it says plainly that the permission table is the whole list and nothing outside it can run. It has no tools. It points and explains; it never changes anything."),
    ("harness", "Under the hood", "One call through the loop", ["Three fixed hooks on every call: before, after, record", "A read runs; a write waits for the person; a bigger change needs a second person", "A tainted session is capped at reads; everything lands on the chain"], "d-harness.png",
     "This is the part that makes an agent safe to run. Every call an agent makes goes through three fixed hooks. Before the call: is there a kill switch, is the tool in the signed catalog, are the arguments valid, and what does policy say. A read runs. A write is parked until the acting person confirms that exact call, once. A bigger change needs a second person's approval. After the call, the result is projected to a declared shape, masked, and scored for injection; if it scores, the session is tainted and capped at reads. And every step lands on a hash-chained record."),
    ("agent-term", "The agent", "A first read, end to end, in a terminal", ["Read the ticket and the last deploy; a cited hypothesis", "A proposal, advisory; the comment parked until confirmed", "A poisoned ticket: proposal refused, write blocked"], "10-term-agent.png",
     "Here is the agent running in a terminal with fakes. It reads the ticket and the last deploy, and produces a first read: the deployment is the likely cause, with two cited claims. It proposes a rollback, and that proposal is advisory. The comment it wants to post is parked; after the person confirms, it is posted once. Then a poisoned ticket, with an instruction hidden in the text: the proposal is refused before the model is even called, and the write is blocked. The chain verifies at the end."),
    ("mcp", "MCP", "The same agent over MCP", ["tools/list is the template's tools, with annotations from their tiers", "A write becomes an elicitation: the client asks the person", "A tainted session is a 403 that names the scope"], "11-term-mcp.png",
     "The same agent can be driven by any MCP client, for example Claude Code. Tools list is the template's tools with annotations from their tiers. A write does not just run: it becomes an elicitation, the client asks the person, and only an accept runs it. Nothing outside the template is callable, and a tainted session is answered with a 403 that names the scope."),
    ("signoff", "Governance", "How a component earns its sign-offs", ["Six stages from scaffolded to on the shelf", "The owner signs by name, an AI security engineer by role", "The hub records; the commit is the signature"], "d-signoff.png",
     "Now governance. A component moves through six stages, and none of them is guessed: each is read from its manifest. Built means the tests are green. Used once means the owner named the project it ran in. Then the owner signs by name, and an AI security engineer signs by role, each at the current version. The hub records the sign-off; an engineer applies it to the manifest and commits, and that commit is the signature. A new version asks both people to sign again."),
    ("queue", "Governance", "The sign-off queue on the hub", ["Every component, its version and both sign-offs", "The form: four attestations and the first real use", "Yours to sign: what waits on you"], "03-signoffs.png",
     "This is the queue under Build. Every component with its version and both sign-offs. The form asks for four attestations: I ran the tests, I ran the example, I read the walkthrough, I read the rules and known limits; and for an owner, where it was first used for real. The server refuses a form with a box missing, so nobody can skip it by calling the API directly."),
    ("onboarding", "Governance", "The onboarding tracker", ["Where each component is, and what has to happen next", "Checklists for a new champion, an owner, an AI security engineer"], "04-onboarding.png",
     "The onboarding tracker shows every component's stage and the next thing that has to happen, and the checklists for the people: a new champion's first week, what an owner keeps, and what an AI security engineer reads before a review."),
    ("assistant", "The assistant", "How the employee assistant answers", ["Sources from the knowledge base, fenced", "Every claim cited, or dropped", "An instruction hidden in a page stops it before the model"], "d-assistant.png",
     "The employee assistant works the same way at a smaller scale. A question is scored, the knowledge base is searched, the pages that match are fenced as sources, and the model answers only from them, in a fixed shape. Every claim is cited or dropped, and the sources are shown with their classification. If a page or the question reads as an instruction, the assistant stops before the model is called and says so."),
    ("kb", "The knowledge base", "Every component has a page the librarian checks", ["A components section, one page per component", "The practices behind them; the skills catalog", "Onboarding: the programme and the component process"], "06-kb-components.png",
     "The knowledge base is a separate product; the collection publishes into it. Every component has a page there, kept healthy by the librarian's checks. Next to them: the practices the components implement, the skills catalog, and the onboarding pages for the programme and for components."),
    ("config", "Inside the bank", "Configured, not rebuilt", ["One file names every system: identity provider, model, secrets, Jira, Azure DevOps, keys, the bucket", "Settings fail closed: a missing gate stops the start", "Fakes cannot reach production"], "13-term-checkconfig.png",
     "How does it run inside the bank? By configuration. One file names every system it touches. Each service checks its configuration before it listens, and a live process with a missing gate never starts. Here the hub's service is told it is in production with mock identity and a fake assistant, and it refuses, naming each problem by its variable. Fakes exist for the sandbox and cannot reach production."),
    ("verify", "Inside the bank", "One script, every gate, any runner", ["The shelf, the vendoring, every test and live example, both services", "The container trees assembled and started", "The same script on GitHub and on Azure Pipelines"], "14-term-verify.png",
     "One script runs every gate: the shelf, the vendored files, every component's tests and live example, both services, the refusal of fakes in production, and the container trees assembled and started. GitHub Actions and Azure Pipelines call the same script, so they cannot drift."),
    ("bundle", "Inside the bank", "Delivered without the internet", ["A release bundle with the hub prebuilt and a sha256 manifest", "Unpacked in a clean directory: every Python gate passes with no network, no Node", "The hub reads its configuration at runtime, so one build runs everywhere"], "15-term-bundle.png",
     "And it is delivered without the internet. The bundle carries everything, including the hub already built, with a manifest of checksums. Unpacked in a clean directory it passes every gate with no network and no Node installed. The hub reads its configuration from the service at runtime, so the same build runs in the sandbox, in staging and in production."),
    ("programme", "The programme", "AI champions: one engineer per team, every two weeks", ["Ten minutes what shipped, twenty a deep dive, fifteen the collection", "One initiative each, from a one-page brief, on a paved road", "Something returned every time"], "08-kb-champions.png",
     "The programme is how this spreads: one engineer per team, every two weeks, forty-five minutes. Each champion carries one initiative, written as a one-page brief filed through the hub, built from the collection on a paved road, and returned as something everyone can reuse."),
    ("start", "Getting started", "Your first hour", ["Sign in to the hub; run one live example from the Tools tab", "Read one practice page", "Draft your brief and bring it to the next meeting"], "05-intake.png",
     "Your first hour: sign in to the hub and run one live example from the Tools tab. Read one practice page. Draft your brief, and bring it to the next meeting. Thank you."),
]
