<p><em>The spark button at the bottom right. It knows who it is talking to and where you are.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="12-the-guide-01.jpg" /></ac:image></p><p><em>The guide on first open: what brings you here, with one choice suggested from your role.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="12-the-guide-02.jpg" /></ac:image></p><p><em>The guide for a leader on an agent's listing: where you are, the suggested next step, the path, and a place to ask.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="12-the-guide-03.jpg" /></ac:image></p><p><em>An answer: quoted from our own pages, each passage attributed, the sources listed.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="12-the-guide-04.jpg" /></ac:image></p><p><em>The tour: the page dimmed, the part that matters lit up, one sentence on it, Back and Next.</em></p><table><tbody><tr><th>You see</th><th>It means</th></tr><tr><td>A round blue button with a spark, bottom right; a small amber dot on it</td><td>The guide. Alt+G opens it too. The dot means a next step is waiting for you.</td></tr><tr><td>A small box beside it: <code>NEXT STEP</code> · <code>New here? Tell me what you're here for and I'll point you at the pages that matter…</code></td><td>The guide speaking first. It shows once per suggestion and goes quiet on its own; the × means "not now" and it will not ask again.</td></tr><tr><td><code>What brings you here?</code> · <code>I'm here to build something</code> · <code>I'm here to understand and decide</code> · <code>I'm here to use the hub</code> · <code>suggested from your role</code></td><td>The first question, asked once. One is highlighted from your role. <code>Change</code> at the top of the panel changes it later.</td></tr><tr><td><code>WHERE YOU ARE</code> and a title</td><td>One line on the page you are on, written for the choice you made. A leader on an agent's page reads that the permission table is the whole list; a builder reads where the evidence links are.</td></tr><tr><td><code>SUGGESTED NEXT STEP</code> · a button · <code>Not now</code></td><td>One suggestion, worked out from your own records: a brief you left half-finished, components waiting for your signature, a page you have not opened. Not now dismisses it for good.</td></tr><tr><td><code>YOUR PATH</code> · <code>2 of 8</code> · steps with a green tick, a blue ring, or grey</td><td>Your journey. Green is done (the hub knows, from what you did), blue ring is the current step, grey is later. Clicking a step takes you there. <code>Mark as done</code> is for steps only you can know about, such as running a command; the command is shown under it.</td></tr><tr><td><code>Show me around</code> · then <code>THE HUB FOR A DECISION · 2 OF 5</code> · <code>Back</code> <code>Stop the tour</code> <code>Next</code> <code>Done</code></td><td>The tour. It moves you page by page and lights up the part that matters, with a sentence on it. Arrow keys move; Escape stops; you keep the page you are on.</td></tr><tr><td><code>ASK</code> with starter chips such as <code>What stops an agent doing something we did not ask for?</code></td><td>Questions people ask most, chosen for your persona. Click one or type your own.</td></tr><tr><td>An answer starting <code>Here is what our own documentation says, in plain terms.</code> or <code>From the repository's own pages:</code>, with <code>— Action tiers and confirmation, The rule in one line</code> under each passage, and <code>SOURCES</code> chips</td><td>The guide quotes our own pages and names them. It never makes up a sentence about the platform. A "Go to" chip after the answer takes you to the page that covers it.</td></tr><tr><td><code>I couldn't find that in our own documentation, so I won't guess…</code></td><td>The pages do not cover it. Ask the programme lead, or it goes on the open-decisions list.</td></tr><tr><td><code>That reads as an instruction rather than a question, so I won't act on it.</code></td><td>Something typed looked like an attempt to steer it. It declines and asks for a question instead.</td></tr><tr><td><code>Answers quote the collection's own pages and name them. The guide never acts on your behalf.</code></td><td>The footer of the panel. It has no tools: it points and explains, it never changes anything.</td></tr></tbody></table><h3>The builder's path</h3><p>See what is on the shelf → Open the reference agent → Run its five-minute example → Write an intake brief → File it with your lead → Build from components → Take your component to the shelf → Get it signed off.</p><h3>The leader's path</h3><p>What this is, in five minutes → What an agent cannot do → The controls, in plain terms (a question) → Who signs, and what they attest → What it costs and who uses it → The decisions that are yours → Ask for a walkthrough.</p><h3>The everyday path</h3><p>Find what you may use → Ask the employee assistant → Request access to something → Say whether an answer helped.</p><ol><li><p><strong>Press the spark button, or Alt+G.</strong><br />The panel opens on the right; the page stays usable beside it.</p></li><li><p><strong>Pick what brings you here.</strong><br />Everything after keeps to that choice. Change it any time at the top of the panel.</p></li><li><p><strong>Take the suggested next step, or press "Show me around".</strong><br />The tour is two minutes and ends where you started.</p></li><li><p><strong>Ask anything, in your own words.</strong><br />The answer names the page it came from; open it if you want the rest.</p></li></ol>