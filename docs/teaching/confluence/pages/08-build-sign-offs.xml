<p><em>Two named people per version. The commit is the signature.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="08-build-sign-offs-01.jpg" /></ac:image></p><p><em>The sign-off queue with the form open in a row: four attestations, the project of the first real use, and the note.</em></p><table><tbody><tr><th>You see</th><th>It means</th></tr><tr><td><code>Component sign-offs</code> · <code>A component reaches the shelf when its owner and an AI security engineer have each signed it at its current version. Sign here; the manifest in the repository is the record and the commit is the signature.</code></td><td>The page's whole idea. The hub records your sign-off; the repository is where it becomes a record.</td></tr><tr><td>Chips: <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Green</ac:parameter><ac:parameter ac:name="title">3 signed</ac:parameter></ac:structured-macro> <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Grey</ac:parameter><ac:parameter ac:name="title">26 awaiting owner</ac:parameter></ac:structured-macro> <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Yellow</ac:parameter><ac:parameter ac:name="title">0 awaiting AI security</ac:parameter></ac:structured-macro></td><td>The state of the shelf at a glance.</td></tr><tr><td><code>The queue</code> · filters <code>All</code> <code>Open</code> <code>Yours to sign</code> <code>Recorded here</code> · columns <code>Component</code> <code>Version</code> <code>Stage</code> <code>Owner</code> <code>AI security</code> · <code>Sign</code></td><td>Every component with its version and both sign-offs. "Yours to sign" shows what waits on you. Sign opens the form in the row.</td></tr><tr><td>Sign-off chips: <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Grey</ac:parameter><ac:parameter ac:name="title">pending</ac:parameter></ac:structured-macro> · <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Green</ac:parameter><ac:parameter ac:name="title">a name · a date</ac:parameter></ac:structured-macro> · <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Yellow</ac:parameter><ac:parameter ac:name="title">awaiting commit</ac:parameter></ac:structured-macro> · <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Red</ac:parameter><ac:parameter ac:name="title">stale: signed at 1.0.0</ac:parameter></ac:structured-macro></td><td>Not yet signed; signed in the repository at this version; recorded here and not yet committed; signed at an older version and needed again.</td></tr><tr><td><code>You</code> card: <code>You may sign as Owner</code> / <code>You sign nothing yet. Owners sign by name … AI security engineers sign by the ai.security role.</code> · <code>How to become either</code></td><td>Who you are to the queue. The owner is the person named in the component; AI security is a role your security lead grants.</td></tr><tr><td>The form: <code>I ran the tests and they are green</code> · <code>I ran the live example</code> · <code>I read the walkthrough end to end</code> · <code>I read the rules it enforces and its known limits</code></td><td>Four attestations. The server refuses a form with one unticked, so they cannot be skipped by calling the API directly.</td></tr><tr><td><code>Used once for real: which project</code> · <code>The owner signs after one real use. Name the project; it is recorded in the manifest.</code></td><td>The owner's extra field on a component's first sign-off. No project, no owner sign-off.</td></tr><tr><td><code>Note (optional)</code> · <code>What you looked at hardest, and anything the owner should fix in the next version.</code></td><td>For the AI security engineer especially: say what you checked.</td></tr><tr><td><code>Recorded: owner sign-off on … 1.0.1.</code> · <code>Export the queue and apply it with the shelf tool; the commit is the signature.</code></td><td>The toast after signing. Your sign-off is a request until an engineer commits it.</td></tr><tr><td><code>Into the repository</code> · <code>A sign-off recorded here is a request until it is in the manifest. Export the queue, apply it, and commit; the shelf tool re-runs the tests before it writes anything.</code> · <code>Download</code></td><td>The engineer's step. The downloaded file is applied with one command; the tool tests again before writing.</td></tr><tr><td><code>A sign-off needs a ready component; this one is draft.</code> · <code>Already signed</code></td><td>Reasons a form will not open: the component is not built yet, or this version already carries your signature.</td></tr></tbody></table><h3>If you are an owner</h3><ol><li>Use the component once for real.</li><li>Run its tests and its example; read its walkthrough and rules.</li><li>Build → Sign-offs → "Yours to sign" → Sign.</li><li>Tick the four boxes, name the project, sign.</li><li>Tell the engineer who commits.</li></ol><h3>If you are an AI security engineer</h3><ol><li>Ask your security lead for the AI security role.</li><li>Read the practices the components enforce.</li><li>For each component: rules, walkthrough, tests, example.</li><li>Sign, and write in the note what you looked at hardest.</li><li>For an agent, read its never list against its tests, one test per line.</li></ol><h3>If you commit it</h3><ol><li>Sign-offs → "Into the repository" → Download.</li><li><ac:structured-macro ac:name="code" ac:schema-version="1"><ac:parameter ac:name="language">bash</ac:parameter><ac:plain-text-body><![CDATA[python3 tools/shelf.py --apply-signoffs shelf-signoffs.json
python3 tools/shelf.py --write
git commit]]></ac:plain-text-body></ac:structured-macro></li><li>The tool re-runs the tests first and refuses if they fail.</li></ol><ac:structured-macro ac:name="warning" ac:schema-version="1"><ac:rich-text-body><p>Nothing here is ever written by hand or invented. A pending sign-off stays pending until a named person gives it, and a new version makes the old signature stale until both sign again.</p></ac:rich-text-body></ac:structured-macro>