<p><em>One page in six sections. It saves as you go, and it is the whole intake.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="07-build-the-intake-brief-01.jpg" /></ac:image></p><p><em>Where a brief begins for someone with no draft: the start screen lists the six sections and one button, Start a brief.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="07-build-the-intake-brief-02.jpg" /></ac:image></p><p><em>After Start: a new draft on section 1, Use case. Name, the problem today, channel and team; the road and the estimate on the right are empty until you write.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="07-build-the-intake-brief-03.jpg" /></ac:image></p><p><em>The demo person's saved draft, which opens where it was left, on section 3. The hub reopens your latest draft at its own step, so this is what a returning person sees, not where a brief starts.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="07-build-the-intake-brief-04.jpg" /></ac:image></p><p><em>The composer, opened from 'Browse the catalog': everything that exists on the left, the brief on the right, the harness locked under 'Required with any tool', and 'What this means' below.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="07-build-the-intake-brief-05.jpg" /></ac:image></p><p><em>Review and file: the whole brief read back, with the harness named beside the tools.</em></p><table><tbody><tr><th>You see</th><th>It means</th></tr><tr><td><code>Start an intake brief</code> · <code>A one-page brief is the whole intake. Everything after it is generated from what you file here.</code> · <code>about 15 minutes · saved as you go</code> · <code>Start a brief</code></td><td>The first screen when you have no draft. The six sections are listed with one line each. Pressing Start creates the draft and opens section 1.</td></tr><tr><td><code>A brief names the team that owns it. You are in no team yet; ask your lead to add you, then start here.</code></td><td>In place of Start a brief, when the platform lists you in no team. A brief cannot be created without the team that owns it; once your lead has added you, the button is back.</td></tr><tr><td>The rail on the left: six numbered sections, ticks on the ones done</td><td>Where you are. You may go back to any section. When you come back to Build later, your latest draft reopens at the section you left it on, not at section 1.</td></tr><tr><td><code>Back</code> · <code>Save draft</code> · <code>Continue to …</code></td><td>Continue checks the section and moves on. Save draft is for leaving; the brief also saves itself about a second after every change.</td></tr><tr><td>Save states: <code>saved</code> · <code>saving…</code> · <code>unsaved changes</code></td><td>Shown near the title. Nothing is lost if you close the tab once it says saved.</td></tr><tr><td><code>This draft was saved from another tab.</code> · <code>Reload to see the latest version; edits made here since are not saved.</code> · <code>Reload the draft</code></td><td>Two tabs edited the same brief. The hub stops saving here rather than overwrite the other tab. Reload and continue.</td></tr><tr><td>Right rail: <code>Recommended road</code> · <code>Estimated cost</code> (<code>model spend</code>, <code>review labour</code>, <code>platform share</code>, <code>basis</code>) · <code>What happens next</code></td><td>Worked out live from what you have written: which road fits, roughly what it costs a month, and what follows filing. They update as you type.</td></tr><tr><td><code>The brief is not complete</code> · <code>Some sections need attention before it can be filed.</code></td><td>A section is missing something. The field is marked; the rail shows which section.</td></tr><tr><td><code>Your team lead files this brief.</code> · <code>Lead confirmation.</code></td><td>If your brief lets an AI change a system rather than read one, only a team lead may file it. The form says so on the review step.</td></tr><tr><td><code>The platform lead confirms the road within two working days; registration, namespaces and the playground follow. Track it in my workspace.</code></td><td>What you see after filing. Nothing else is asked of you until the lead answers.</td></tr></tbody></table><ac:structured-macro ac:name="anchor" ac:schema-version="1"><ac:parameter ac:name="">brief-sections</ac:parameter></ac:structured-macro><h2>The six sections, field by field</h2><h3>1. Use case (name, problem, channel)</h3><p><code>Name</code>: one line, in the words of the desk that will use it. <code>The problem today</code>: what happens now, by hand, and how long it takes. <code>Channel</code>: who talks to it (operator, customer, partner, batch); customer and partner channels open on road R3. <code>Team</code>: the team that owns it and its cost centre; when you are in no team the field says <code>You are in no team yet. Ask your lead to add you; a brief needs the team that owns it.</code> and the section will not pass until you are.</p><h3>2. People (owner, expert, allowance)</h3><p><code>Business owner</code>: accountable for the outcome; signs the monthly review. <code>Product owner</code>: runs the build and the sandbox with the embedded engineer. <code>Domain expert</code>: labels the evaluation cases. <code>Labelling allowance</code>: hours a week the expert gives; under one hour stalls the corpus gate.</p><h3>3. Data and tools (systems, tools, classes, tier)</h3><p><code>Systems of record</code>: pick from the registry (<code>+ add a system</code>); a system without a recorded contract cannot be named. <code>Tools needed</code>: what it calls, each with its tier; <code>Browse the catalog</code> opens the composer (below). <code>Reused from what exists</code>: the components and services the consumer builds on, once you have chosen some. <code>Data classes read</code>: internal, confidential, restricted. <code>Tier ceiling</code>: Read, Write with confirmation, or Write with approval: the most it may ever do.</p><h3>4. Model (need and ceiling)</h3><p><code>Model need</code>: No generative model, Utility, Workhorse or Frontier. <code>Classification ceiling for the model</code>: the highest class of data it may see. <code>Substitution</code>: whether the platform may swap the model for an equivalent.</p><h3>5. Outcome (metric, baseline, target)</h3><p><code>Outcome metric</code>: one number it is meant to move, for example minutes per case. <code>Unit</code>, <code>Baseline today</code>, <code>Target</code>, <code>Baseline measured on</code>: older than a quarter and the lead asks for a fresh one.</p><h3>6. Review and file (road, cost, what happens next)</h3><p>The whole brief read back, the road to confirm, the estimate, and one box: <code>I have read what happens next and the estimated cost</code>. Then <code>File</code>.</p><ac:structured-macro ac:name="anchor" ac:schema-version="1"><ac:parameter ac:name="">composer</ac:parameter></ac:structured-macro><h2>The composer: build from what exists</h2><table><tbody><tr><th>You see</th><th>It means</th></tr><tr><td><code>Compose from what exists</code> · <code>Drag a system, a tool or a component onto the brief; Add works too. It saves as you go.</code></td><td>The dialog "Browse the catalog" opens in section 3. Left: everything that exists. Right: your brief's section 3. Nothing here is a separate model; it edits the brief directly.</td></tr><tr><td>Search box and filters <code>All</code> <code>Systems</code> <code>Tools</code> <code>Collection</code> <code>Services</code></td><td>Narrow the palette. It only lists what your role may see.</td></tr><tr><td>Groups: <code>Systems of record</code> · <code>Tools</code> · <code>The collection</code> · <code>The bank's services</code></td><td>The registry's systems (each needs a recorded contract), the tools each system offers with their tier and data classes, the collection's reusable components, and the assistants, agents and knowledge services already on the platform.</td></tr><tr><td>A row with a grip <code>⋮⋮</code>, a chip such as <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Grey</ac:parameter><ac:parameter ac:name="title">R</ac:parameter></ac:structured-macro> <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Yellow</ac:parameter><ac:parameter ac:name="title">W1</ac:parameter></ac:structured-macro> <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Green</ac:parameter><ac:parameter ac:name="title">GA</ac:parameter></ac:structured-macro> <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Yellow</ac:parameter><ac:parameter ac:name="title">preview</ac:parameter></ac:structured-macro>, and <code>+ Add</code> or <code>✓ in the brief</code></td><td>Drag the row onto the right side, or press Add. GA on a component means two named people signed this version; preview means it is still earning them. "In the brief" means it is already there.</td></tr><tr><td>A greyed row: <code>no recorded contract yet; a first consumer cannot name it</code></td><td>That system cannot be added until its owner records a contract. Dragging it does nothing.</td></tr><tr><td><code>Drop anything here</code> / <code>Drop to add it to the brief</code></td><td>The whole right side is the drop zone; it lights up while you drag.</td></tr><tr><td><code>Tools 4 of 15</code> · <code>Reused from what exists</code></td><td>Your tools against the 15-tool session ceiling, and the components and services you are building on. The × on any chip or row removes it.</td></tr><tr><td><code>WHAT THIS MEANS</code>: <code>Highest tier so far: W1 · the ceiling is Read, which does not cover it.</code> <code>Set the ceiling to Write with confirmation</code></td><td>The consequences, live. One button per fix: raise the ceiling, tick the internal and confidential classes the tools read. Restricted is never a tick away (next row). It also says whether a lead will have to file, how many tools you have left, and that a money tool is refused for a first consumer.</td></tr><tr><td><code>… reads restricted data, which a first consumer cannot use</code>; <code>remove it or take the use case to the platform team for a data-class exception</code></td><td>The one line with no fix button. A tool that reads restricted data cannot be in a first consumer's brief: remove the tool, or take the use case to the platform team for a data-class exception.</td></tr><tr><td><code>Required with any tool · the harness</code>: five locked rows, <ac:structured-macro ac:name="status" ac:schema-version="1"><ac:parameter ac:name="colour">Blue</ac:parameter><ac:parameter ac:name="title">required</ac:parameter></ac:structured-macro> and a 🔒: Governed action loop, Untrusted input guard, Cited LLM engine, Audit chain, Ids-only logging · <code>Every tool runs inside the governed action loop; the guard, the engine, the chain and ids-only logging come with it.</code></td><td>The harness baseline. It appears as soon as the brief names a tool and cannot be removed. Filing writes it into the brief, on the hub and again on the platform, so no solution that calls a tool is ever recorded without its harness. In the palette these components read as <code>required</code> rather than offering Add. The review page lists them under <code>harness</code>.</td></tr><tr><td><code>cos_get_case brought COS · case notes with it</code></td><td>Dropping a tool adds its system of record too, when that system has a contract.</td></tr><tr><td><code>Done</code></td><td>Closes the composer. Everything is already in the brief and saving itself.</td></tr></tbody></table><ol><li><p><strong>In section 3, press "Browse the catalog".</strong><br />The composer opens over the brief.</p></li><li><p><strong>Drag a tool onto the right side, or press Add.</strong><br />Its system comes with it. The tier chip tells you what the tool may do.</p></li><li><p><strong>Read "What this means" and press the fix buttons.</strong><br />The ceiling and the internal and confidential classes are set for you; the road on the brief's right rail updates. A tool that reads restricted data has no button: remove it, or take the use case to the platform team.</p></li><li><p><strong>Add the components and services you build on.</strong><br />A signed one is review time saved; it appears under "Reused from what exists" in the brief and on the review page.</p></li><li><p><strong>Press Done and continue to section 4.</strong><br />Nothing else to save.</p></li></ol><ac:structured-macro ac:name="anchor" ac:schema-version="1"><ac:parameter ac:name="">brief-flow</ac:parameter></ac:structured-macro><h2>Filing it, step by step</h2><ol><li><p><strong>Build → "Start a brief".</strong><br />A draft is created and section 1 opens. From now on it saves itself.</p></li><li><p><strong>Fill each section and press "Continue".</strong><br />Continue checks the section with the same rules the server applies, so you never reach an error you could not see.</p></li><li><p><strong>Watch the right rail.</strong><br />The recommended road and the estimate update as you write. If the road surprises you, section 3's tier ceiling is usually why.</p></li><li><p><strong>On "Review and file", tick the confirmation and press "File the brief".</strong><br />If a lead has to file it, hand it over: your draft is in their workspace under the team's consumers.</p></li><li><p><strong>Read the message and go to My workspace.</strong><br />The lead confirms the road within two working days. The consumer then appears under your team's consumers at its stage.</p></li></ol>