<p><em>The six roads, the collection, the programme, and the fifteen-minute example.</em></p><p><ac:image ac:width="900"><ri:attachment ri:filename="10-learn-01.jpg" /></ac:image></p><p><em>Learn: the six roads, the collection and the knowledge base, the fifteen-minute example, office hours.</em></p><table><tbody><tr><th>You see</th><th>It means</th></tr><tr><td><code>The six roads</code> · <code>pick one in the brief; the repository, gates and reviews follow from it</code></td><td>A table of Road, For and Time. R1 tools and knowledge (no model of its own). R2 an internal agent for staff. R3 a customer and partner assistant. R4 a batch agent with a review queue. R5 a retrieval service with provenance on every chunk. R6 onboarding an AI solution that already exists.</td></tr><tr><td><code>The collection and the knowledge base</code>: cards <code>Reusable components</code> · <code>The knowledge base</code> · <code>AI champions</code></td><td>What the collection is (tools, integrations and patterns lifted from products in production, each with a five-minute README, tests, a version and two sign-offs), where the practice pages live, and the programme in one paragraph. Links: <code>Open Discover</code>, <code>Sign-offs</code>, <code>Open the knowledge base</code>, <code>Start a brief</code>, <code>Onboarding</code>.</td></tr><tr><td><code>The 15-minute example</code></td><td>Three steps: get a playground key from My workspace; run the example against the sandbox gateway (every call is attributed to you); change one tool and run again to see the policy decision, the audit entry and the cost.</td></tr><tr><td><code>Office hours and bootcamp</code></td><td>The weekly office hour, the two-day bootcamp at registration with your embedded engineer, and who to escalate to.</td></tr></tbody></table>