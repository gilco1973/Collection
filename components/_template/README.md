# {{name}}

{{summary}}

## What it is for

One paragraph: the problem this solves and the situation an engineer is in when they reach for it.

## Five-minute start

```
# the smallest thing that shows it working, copy-pasteable
```

## What is inside

| File | What it is |
| --- | --- |
| `...` | ... |

## How to reuse it

Copy the directory (or the one file) into your project. State what has to be wired (a transport, a secrets provider, a
connection) and what stays as is. No hidden dependency: if it imports something outside its directory, say so here and
in `component.json` (`requires` or `vendored`).

## Rules it enforces

The invariants a consumer cannot switch off, in one list. If none, delete this section.

## Where it came from

Project, path and snapshot date; decisions taken while extracting it (renames, removed project-specific parts).

## Known limits

What it does not do and what a later version should add.
