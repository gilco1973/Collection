# Handover: <delivery>

Date: <YYYY-MM-DD> (first issued <date>). Author: <name>. Branch `<branch>`, folder `<folder>`, built to <the
specification and its version>. <What it depends on, or "depends on nothing".>

| Version | Date | Commit | What it contained |
| --- | --- | --- | --- |
| v1 | | | |

## What this is

<One paragraph.>

```
<the commands that run it: tests, demonstrations, checks>
```

Last run on this machine: <n> tests passed; <the demonstration results with numbers>.

## Since the last handover (<date> → <date>)

| Change | Where | Commit |
| --- | --- | --- |
| | | |

## Tickets this work completes, in the self-contained sense

"Completes" means the code, tests and behaviour the ticket describes exist and pass here, with a fake in place of the
company service where one is needed. The account-bound step that closes the ticket is named per row.

### <EPIC-1> · <name>

| Ticket | Done here | Remains on the company's account |
| --- | --- | --- |
| | | |

## Decisions taken while building (for the specification's next revision)

- **<decision id>:** <what was decided and why>. Reverse by <how>.

## Layout of this folder

```
<tree with one line per directory>
```

## What it does not do

<What was never exercised against real services; what is a heuristic; what was verified by proxy.>

## Taking it to staging

1. <step>
2. <step>
